using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Labb : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Input.GetAxis();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
